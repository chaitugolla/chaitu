#include <stdio.h>
int main()
{
    int a,b;
    printf("enter a value:");
    scanf("%d",&a);
    printf("enter b value:");
    scanf("%d",&b);
    if(a==b)
    {
        printf("both are same values");
    }
    else{
        printf("both are not same");
    }
    return 0;

}
